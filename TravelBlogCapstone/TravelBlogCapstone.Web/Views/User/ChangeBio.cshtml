@model TravelBlogCapstone.Web.Models.ApplicationUser

@{
    ViewBag.Title = "Change Biography";
}

<h2>@ViewBag.Title</h2>
<hr />
@using (Html.BeginForm("ChangeBio", "User", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary("", new { @class = "text-danger" })
    <div class="form-group">
        @Html.LabelFor(m => m.Bio, new { @class = "col-md-2 control-label" })
        <div class="col-md-5">
            @Html.TextAreaFor(m => m.Bio, new { @class = "form-control", id="change-bio"})
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-5">
            <input type="submit" value="Change Biography" class="btn btn-default" />
        </div>
    </div>
}
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}